"""Built-in analyzers.

Every module in this package is scanned at startup and any concrete
:class:`~imgintel.core.analyzer.Analyzer` subclass defined in it is registered
automatically. Adding a feature means adding a file here — nothing else.
"""
